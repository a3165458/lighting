# Bundled into Electron portable/setup (do not commit binaries).

Place here before packaging:
- lighting-host.exe  (from scripts/build-windows.ps1)
- Lighting.apk       (from scripts/build-android.ps1)

Or run: ..\scripts\build-electron-win.ps1
